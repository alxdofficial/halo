# model

The HALO model implementation: a physical-Hz **filterbank tokenizer** (rate-invariant,
channel-independent) → **dual-branch encoder** → **per-patch language-alignment head** that maps
IMU patches into the frozen-SBERT label-text space.

To be rebuilt into this repo — see the roadmap in the root [`README.md`](../README.md).
